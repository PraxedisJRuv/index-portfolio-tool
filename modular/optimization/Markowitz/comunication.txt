La tercer opción es la elegido ya que aglomera todo mejor, corre bastante rápido y no da a problemas de traducción
el mayor inconveniente es que hay que trabajar para usarla apropiadamente, sincronizando los scripts para solicitar
los datos entre python y dejando C++ solo a optimizar, debería ser el go to.

Hay que revisar bien las opciones para MArkowitz ya que si bien entiendo, KKT es bastante rápido pero no muy eficiente


#1er opcion
#c++ escribe
ofstream file("weights.csv");

for(int i=0;i<n;i++)
    file << w(i) << endl;

#python lee
import numpy as np

w = np.loadtxt("weights.csv")

#2da opcion
#correr desde python
import subprocess

subprocess.run(["./portfolio"])

import numpy as np
w = np.loadtxt("weights.csv")


#3er opcion
#include <pybind11/pybind11.h>
#include <pybind11/eigen.h>

VectorXd optimizar(...)

PYBIND11_MODULE(portfolio_opt, m) {
    m.def("optimizar",&optimizar);
}
import portfolio_opt

w = portfolio_opt.optimizar(Sigma,wb,w0,tau)